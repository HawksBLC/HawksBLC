backend:
  name: github
  repo: HawksBLC/hawksblc
  branch: main

media_folder: images
public_folder: images

collections:
  - name: events
    label: Events
    folder: _events
    create: true
    slug: "{{slug}}"
    fields:
      - { label: Event Name, name: title, widget: string }
      - { label: Date, name: date, widget: datetime, format: "MMMM D, YYYY" }
      - { label: Time, name: time, widget: string, required: false }
      - { label: Location, name: location, widget: string }
      - { label: Description, name: description, widget: text, required: false }
      - label: Event Type
        name: type
        widget: select
        options: [Game Night, Fundraiser, Meeting, Banquet, Community Event, Other]
      - { label: Registration Link, name: register_url, widget: string, required: false }
      - { label: Event Banner Image, name: image, widget: image, required: false }

  - name: sponsors
    label: Sponsors
    folder: _sponsors
    create: true
    slug: "{{slug}}"
    fields:
      - { label: Sponsor Name, name: title, widget: string }
      - label: Tier
        name: tier
        widget: select
        options: [Platinum, Gold]
      - { label: Website URL, name: url, widget: string }
      - { label: Logo, name: logo, widget: image, required: false }

  - name: board
    label: Board of Directors
    folder: _board
    create: true
    slug: "{{slug}}"
    fields:
      - { label: Name, name: title, widget: string }
      - label: Role
        name: role
        widget: select
        options: [President, Vice President, Treasurer, Secretary, Director]
      - { label: Email, name: email, widget: string, required: false }
      - { label: Phone, name: phone, widget: string, required: false }

  - name: settings
    label: Site Settings
    files:
      - label: General Settings
        name: general
        file: _data/settings.yml
        fields:
          - { label: Site Title, name: title, widget: string }
          - { label: Contact Email, name: email, widget: string }
          - { label: Facebook URL, name: facebook, widget: string }
          - { label: Twitter Handle, name: twitter, widget: string }
          - { label: Donation Link, name: donations, widget: string }
